import React from 'react'

const UserContext = React.createContext()

export default UserContext;

//This is the starting point. You create a "Context" using React.createContext(). Think of this as an empty global bucket that will eventually hold your data.