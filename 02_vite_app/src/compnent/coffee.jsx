//file name can't be coffee.js it does not work in vite react

function Coffee(){//function but start with a capital letter general convention in vite-react
    return(
        <h3>
            coffee is ready
        </h3>
    )
}

export default Coffee//export has to be same as the function name

//now I'm going to app.jsx for the import